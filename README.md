# ostad-assignment-of-module-6
# Setup Guide
* Clone the repository
* Run the command `npm install`
* Run the command for start the server and project `php -S localhost:8888`
* Then visite localhost:8888 and test the project